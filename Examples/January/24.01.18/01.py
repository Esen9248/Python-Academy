import math 
# from datetime import datetime, timedelta, date
from datetime import (
	datetime,
	timedelta,
)
print(math.sqrt(9))
print(math.fabs(-6))
print(datetime.now())
print(datetime.now().weekday())